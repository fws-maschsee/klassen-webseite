name: klassen-webseite-anmeldung

services:
  # Ohne Volume: jeder Lauf braucht die Ersteinrichtung, sonst passt das PAT in pat/ nicht mehr.
  db:
    image: postgres:17.11-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zitadel
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d zitadel']
      interval: 1s
      timeout: 5s
      retries: 60

  zitadel:
    # Dieselbe Fassung wie in Produktion (server-config); nur gemeinsam anheben.
    image: ghcr.io/zitadel/zitadel:v4.19.1
    # Der Masterkey muss genau 32 Zeichen haben; Testwert, kein Geheimnis.
    command: 'start-from-init --masterkey MasterkeyNeedsToHave32Characters --tlsMode disabled'
    environment:
      ZITADEL_DATABASE_POSTGRES_HOST: db
      ZITADEL_DATABASE_POSTGRES_PORT: '5432'
      ZITADEL_DATABASE_POSTGRES_DATABASE: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_USERNAME: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE: disable
      ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE: disable

      # Muss zeichengleich zu OIDC_ISSUER passen: `jose` prüft `iss` exakt.
      ZITADEL_EXTERNALSECURE: 'false'
      ZITADEL_EXTERNALDOMAIN: localhost
      ZITADEL_EXTERNALPORT: '${ZITADEL_PORT:-8080}'
      ZITADEL_TLS_ENABLED: 'false'

      ZITADEL_FIRSTINSTANCE_ORG_NAME: Testschule

      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_USERNAME: ci-admin
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_NAME: CI Admin
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_PAT_SCOPES: 'openid profile email urn:zitadel:iam:org:project:id:zitadel:aud'
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_PAT_EXPIRATIONDATE: '2099-01-01T00:00:00Z'
      ZITADEL_FIRSTINSTANCE_PATPATH: /pat/ci-admin.pat
    volumes:
      # Bind-Mount, damit der Testprozess das PAT ohne Griff in den Container liest.
      - ./pat:/pat
    ports:
      - '127.0.0.1:${ZITADEL_PORT:-8080}:8080'
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ['CMD', '/app/zitadel', 'ready']
      interval: 1s
      timeout: 5s
      retries: 120
      start_period: 5s
