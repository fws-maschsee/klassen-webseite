/** Siehe `shipyard-base.d.ts` für den Grund dieser Deklaration. */

/** remark-Plugin: aktiviert `:::`-Direktiven. */
export declare const remarkDirective: () => unknown

/** remark-Plugin: rendert `:::note` & Co. als Admonition. */
export declare const remarkAdmonitions: () => unknown
